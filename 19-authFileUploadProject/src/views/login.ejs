<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Login</title>
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body class="bg-black min-h-screen flex items-center justify-center p-3">
	<div class="w-full max-w-xs bg-gray-800 rounded-lg shadow-xl p-5">
		<div class="text-center mb-4">
			<h2 class="text-xl font-semibold text-gray-100">Sign In</h2>
		</div>
		<form action="/api/v1/login" method="post" class="space-y-3">
			<div class="relative flex items-center">
				<i class="fas fa-envelope absolute left-3 text-gray-400 text-sm"></i>
				<input type="email" name="email" placeholder="Email" required
					class="w-full pl-9 pr-3 py-2 bg-gray-700 text-gray-100 rounded-md text-sm border-0 focus:ring-1 focus:ring-blue-500">
			</div>
			<div class="relative flex items-center">
				<i class="fas fa-lock absolute left-3 text-gray-400 text-sm"></i>
				<input type="password" name="password" placeholder="Password" required
					class="w-full pl-9 pr-3 py-2 bg-gray-700 text-gray-100 rounded-md text-sm border-0 focus:ring-1 focus:ring-blue-500">
			</div>
			<button type="submit" class="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 text-sm">Sign
				In</button>
		</form>
		<p class="text-center text-xs text-gray-400 mt-4">
			No account? <a href="/register" class="text-blue-400 hover:text-blue-300">Register</a>
		</p>
	</div>
</body>

</html>
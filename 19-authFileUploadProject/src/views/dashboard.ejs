<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Dashboard</title>
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body class="bg-black min-h-screen flex items-center justify-center p-3">
	<div class="w-full max-w-xs bg-gray-800 rounded-lg shadow-xl p-5">
		<div class="text-center">
			<div class="relative inline-block mb-3">
				<img src="<%= imageUrl %>" alt="Profile" class="w-16 h-16 rounded-full object-cover ring-2 ring-gray-600">
				<div class="absolute bottom-0 right-0 bg-blue-600 text-white p-1 rounded-full">
					<i class="fas fa-user text-xs"></i>
				</div>
			</div>
			<h2 class="text-lg font-semibold text-gray-100 mb-1">
				<%= fullname %>
			</h2>
			<p class="text-sm text-gray-400 mb-4">
				<%= email %>
			</p>
			<form action="/logout" method="POST">
				<button type="submit"
					class="inline-flex items-center justify-center space-x-2 px-4 py-2 bg-red-600 text-white text-sm rounded-md hover:bg-red-700">
					<i class="fas fa-sign-out-alt"></i>
					<span>Logout</span>
				</button>
			</form>
		</div>
	</div>
</body>

</html>